import SwiftUI

/// A namespace for design constants used throughout the v2 app.
enum DS {
    static let corner: CGFloat = 14
    static let spacing: CGFloat = 12
    static let shadow = Color.black.opacity(0.12)
}
